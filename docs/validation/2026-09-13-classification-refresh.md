# Classification continuity and requested refresh

Date: 2026-09-13

State: **As-built**

## Scope

Private saved state, explicit user corrections, protected agent choices and
requested refresh using current native captures. Boundary classification:
unreleased — corrected in place. Existing work-map version 1 and the bundled
viewer remain the rendering contract; no user snapshots were migrated.

## Executable evidence

The Node suite exercises current facts with saved choices, source-local key
changes, conflicting native identities, independent user target ownership,
absent/reappearing issues, pending review across repeated refresh, unknown
context, malformed state/choices, actor restrictions, and output preservation.
The Chromium continuity case checks saved grouping alongside current completed
status and verifies that absent remembered work does not enter HTML or search.

- `just ci`: passed, including 31 Node tests and documentation, format and lint checks.
- `just browser-check`: passed, 15 Chromium tests using synthetic maps.
- The skill-creator quick validator accepted the updated skill.

Clean-checkout and hosted CI evidence is recorded in the PR handoff.

## Independent skill execution

An independent agent explicitly read the updated skill, authored its own Korean
taxonomy from invented mixed-source input, and executed normalize, validate,
render, remember, revise, refresh, classify, validate and render through root
Just commands. The evaluator did not read implementation tests for an answer.

- The user's selected research-tooling category and shared-tool target persisted
  with user ownership while the source status changed to completed.
- Changed full description triggered one pending agent classification. After
  reconsideration, no assigned classification remained pending.
- One no-longer-observed context issue was omitted from the current map and
  retained in private state; it was not inferred deleted or completed.
- Final counts: 4 assigned, 0 context, 3 registered relations; 5 remembered
  identities. Embedded HTML data matched the generated work map, and the state
  map matched it as well.

The evaluator's interactive browser open was denied by host URL policy and was
not bypassed. Its evidence is generation and static artifact inspection;
repository Chromium verification is separate. Canonical documentation was being
updated during the evaluation; the stale Target wording it identified was aligned
before handoff. No live source collection, private report reuse, skill relinking,
package publication or automatic discovery was performed in this follow-up.
