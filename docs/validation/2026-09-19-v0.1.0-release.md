# Stellar v0.1.0 release — 2026-09-19

## Published identity

The first public [GitHub Release](https://github.com/weirdry/stellar/releases/tag/v0.1.0)
uses immutable annotated tag `v0.1.0` on commit
`b77b007ee0e9a217545c7860ad99a5b85d50b794`. The tag object is
`5451638edfaab0f1e43a8dd59ed836b18e049c2b`. The release was published on
2026-09-19 in Asia/Seoul (2026-09-18 UTC).

The maintainer authorized promotion and publication. `main` was fast-forwarded
from `73acc63` to the validated `dev` commit; no history was rewritten.
Release preparation only changed documentation relative to the reviewed
implementation at `8916cf9`. The public tag was installed and checked before
the GitHub Release was published. This record is post-publication evidence;
adding it does not move or replace the tag.

The distribution artifact is the GitHub revision, including the runner,
schemas, viewer, references, MIT license and dependency notices. No Stellar
npm package, separate upload bundle, hosted service or user report was published.

## Gates at the release commit

| Check                                                                              | Result                                                             |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Local `just ci` and commit hook                                                    | 69/69 Node; documentation, format, lint and bundle currency passed |
| [dev CI 35359299843](https://github.com/weirdry/stellar/actions/runs/35359299843)  | 69/69 Node, 47/47 Chromium; bundle current                         |
| [main CI 35359548502](https://github.com/weirdry/stellar/actions/runs/35359548502) | 69/69 Node, 47/47 Chromium; bundle current                         |

Both hosted runs used `b77b007`. Local Chromium was not repeated for the
documentation-only release preparation. Hosted Chromium uses the repository's
pinned Playwright and synthetic fixtures; it is not manual host-browser evidence.

## Published installation acceptance

Each installation ran in a fresh disposable Linux arm64 container using Node
24.21.0, npm 11.19.0, Git 2.39.5 and official `skills@1.7.0` (the version resolved
by the unpinned npx command during this run). The Docker base was
`node:24-bookworm` at digest
`sha256:6dac556d980b7f0e5498d08f08cee0ca67798b4ad6c23964a9214920e67758d0`.

The normal command was exercised with explicit agents and confirmation flags
for non-interactive execution:

```sh
npx --yes skills add weirdry/stellar --skill stellar --agent codex claude-code -g -y
npx --yes skills add https://github.com/weirdry/stellar/tree/v0.1.0 --skill stellar --agent codex claude-code -g -y
```

The two commands ran in separate containers. Both installed the canonical skill
and Claude Code link, without `.git` or `node_modules`. All 121 installed files
matched the release commit byte-for-byte; default-branch and tag installations
also matched each other. The installed runner's SHA-256 was
`cb219c96b6c9ed6f35a098fd776d6f6e343e1434301d0df4aa47b9b2236192cb`.

An exact-ref preflight at `8916cf9` had the same runner bytes and also passed.
The published-source checks above establish the actual release acceptance;
the preflight alone did not establish it.

## Installed workflow

From an unrelated task directory, an acceptance script invoked the installed
runner with an empty tool/dependency lookup environment. It used only the
tracked invented mixed-source capture and classification choices.
Each installation passed all 19 expected command outcomes:

- Help, normalization, issue/body inspection, bounded reading and literal search.
- Initial classification, response retention, validation and HTML rendering.
- All four `verify-run` checks: capture facts, embedded map, bundled viewer and
  saved state.
- Initial state creation, an explicit user revision and a subsequent refresh.
  The user's classification survived with `origin: user` and one preserved-user
  entry; an agent attempt to overwrite it was refused.
- An allowed agent decision, rendering and all four checks on the final run.
- Refusal to write into an occupied run directory, with prior output untouched.

Capture bytes, initial and revised runs, final output after refusal, and all
installed files remained unchanged. This was manual command replay, not a fresh
Codex or Claude Code invocation. The installer had Git/npm available in its
container; runtime commands used absolute Node paths and an empty environment.
Earlier no-development-tools runtime evidence remains in the
[installation record](2026-09-18-skill-installation.md).

## Limits and preservation

Fresh host discovery, live collection, macOS/Windows installation and manual
visual inspection were not performed. Classification meaning is not approved by
artifact verification. Remaining viewer improvements are tracked in
[#16](https://github.com/weirdry/stellar/issues/16), and optional classification
suggestions in [#10](https://github.com/weirdry/stellar/issues/10).

Only public synthetic data entered the disposable environments. Existing user
skill registrations, reports, saved states and source systems were not modified.
Publication and acceptance are tracked in
[#19](https://github.com/weirdry/stellar/issues/19).
