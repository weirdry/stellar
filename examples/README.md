# Examples

Future examples must use synthetic, shareable issue data. Real issue snapshots
and user reports belong in ignored `local/` and `outputs/` directories.

Add the first example with the real input schema and renderer. It must use a
different owner and taxonomy from the original prototype, exercising reuse
without viewer code changes. See [quality scenarios](../docs/architecture/10-quality.md).
