# Work-map schema

State: **Open**

The final input schema will be defined when the viewer and renderer are imported.
No machine-readable contract or released schema version exists yet.

The contract must represent source facts, classifications, relations, provenance,
and report metadata without embedding one user's taxonomy into the viewer.
See [cross-cutting concepts](../docs/architecture/08-crosscutting-concepts.md).
